#include "main.h"

/**
 *factorial- function for the factorial of number
 *@n: number
 *Return: none
 */

int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
